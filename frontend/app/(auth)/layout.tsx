
/**
 * Layout para las páginas de autenticación.
 * Proporciona un contenedor centrado y estructura común.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
    return <div>{children}</div>;
}